<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingStart="@dimen/collection_item_horizontal_spacing"
    android:paddingTop="@dimen/queue_item_vertical_spacing"
    android:paddingEnd="@dimen/collection_item_horizontal_spacing"
    android:paddingBottom="@dimen/queue_item_vertical_spacing">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:contentDescription="@string/add_to_queue"
        android:paddingEnd="@dimen/collection_item_horizontal_spacing"
        android:src="@drawable/baseline_folder_open_24"
        app:tint="?android:textColorPrimary" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?textAppearanceListItem"
        android:textColor="?android:textColorPrimary"
        tools:text="Dead Can Dance" />

</LinearLayout>
