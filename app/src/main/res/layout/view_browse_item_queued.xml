<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/status_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/add_to_queue"
        android:paddingStart="@dimen/collection_item_horizontal_spacing"
        android:paddingTop="@dimen/collection_item_vertical_spacing"
        android:paddingEnd="@dimen/collection_item_horizontal_spacing"
        app:tint="?colorAdd"
        tools:src="@drawable/baseline_playlist_add_24" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/status_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:textAppearance="?textAppearanceButton"
        android:textColor="?colorAdd"
        tools:text="Add to queue" />

</LinearLayout>
